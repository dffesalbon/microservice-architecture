spring:
  profiles:
    active: dev
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8005
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false